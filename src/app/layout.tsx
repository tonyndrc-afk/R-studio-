import './globals.css';
export const metadata={title:'R STUDIO — RÉTIF',description:'MVP de production visuelle IA'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="fr"><body className="font-sans"><main className="mx-auto max-w-7xl p-6">{children}</main></body></html>}
